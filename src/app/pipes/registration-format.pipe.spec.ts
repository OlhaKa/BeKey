import { RegistrationFormatPipe } from './registration-format.pipe';

describe('RegistrationFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new RegistrationFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
